# RFC: <PROJECT_NAME>

<!-- [🤖 agent] generates this file locally via `/pw-rfc` — always, regardless of whether you
     publish anywhere external (Generate vs Publish, see docs/RFC.md). Wave 1 (`/pw-rfc <slug>`,
     gated on analysis `approved ✅`) fills Background → Dependencies. Wave 2
     (`/pw-rfc <slug> milestone`, gated on PLAN `approved ✅`) fills Milestone + Conclusion.
     Rollout/Rollback Plan are filled only if you ask. Glossary and the References subsections stay
     human-owned placeholders — the agent never fills those. Legend: 🤖 agent · 🧑 you. -->

- **Status:** draft
- **Backend:** <markdown | lark | …, from rfc/META.md>
- **Target:** <external doc ref, from rfc/META.md — blank if not yet published>
- **Wave 1 published:** no
- **Wave 2 published:** no

## Glossary
🧑 <terms/acronyms specific to this RFC — fill in if useful; the agent doesn't populate this>

## Background
🤖 <analysis §1 Problem/goal + §2 Current state — what's true today, and why this needs to change now>

## Requirements
🤖 <what the solution must satisfy — from analysis §1 goal + context/REQUIREMENTS.md if present>

### Out of scope
🤖 <analysis §6 Out of scope, verbatim intent>

## Solution

### Approach #1 (Preferred)
🤖 <analysis §4 Proposed approach — the recommended path>

#### Overview (Optional)
#### Block Diagram (Optional)
<!-- 🤖 auto-generated by default (mermaid from analysis, rendered via an isolated sub-agent call)
     — the section heading says "(Optional)" because that's the template's own wording (you may
     delete the subsection if you don't want it), not because generation is conditional; on a
     rendering failure this stays a placeholder and the rest of the RFC still publishes — see
     docs/RFC.md -->
#### Sequence Diagram (Optional)
<!-- same auto-generate-by-default + isolation/degrade rule as Block Diagram -->
#### Database Modelling (Optional)
#### APIs (Optional)

### Approach #2
🤖 <analysis §4 alternatives, if any were considered — omit this subsection entirely if there
   weren't. If publishing to a backend whose template pre-populates this section (e.g. Lark's real
   template ships a placeholder here regardless), leave that existing content untouched rather than
   deleting it — don't guess at a removal operation that wasn't asked for.>

#### Overview (Optional)
#### Block Diagram (Optional)
#### Sequence Diagram (Optional)
#### Database Modelling (Optional)
#### APIs (Optional)

## Dependencies
🤖 <analysis §3 Affected repos & surfaces — cross-repo/cross-service dependencies this touches>

## Milestone (Optional)
🤖 <filled at Wave 2, from task/PLAN.md's dependency-DAG groups + task table + SP/timeline estimate>

### Milestone #1
### Milestone #2

## Rollout Plan (Optional)
🧑🤖 <filled only if you ask — from analysis risks + PLAN's rollback-plan-if-a-group-fails>

## Rollback Plan (Optional)
🧑🤖 <filled only if you ask>

## Conclusion
🤖 <filled at Wave 2 — the recommendation, restated for a reader who skipped straight here>

## References

### Open questions?
🧑 <RFC-doc comments pulled read-only via `/pw-rfc <slug> comments` land in
   analysis/review/RFC.review.md instead of here — this section is for questions you want to
   record directly in the RFC itself>

### RFC review meeting notes
🧑 <human-owned — the agent never writes here>
