# CLAUDE.md — tooling (maintainers)

The agent entrypoint for this directory lives in AGENTS.md (provider-neutral) — imported below so
Claude Code picks it up. Keep guidance in AGENTS.md, not here, so every agent tool shares one copy.

@AGENTS.md
