pw-common.sh	infrastructure dot-sourced by every script; referenced as a name, never a command
pw-teardown.sh	worktree-remove delegate (invoked via pw-lib command surface)
pw-doctor.sh	user-facing maintenance command referenced by name in commands
pw-lib.sh	core library, *the* documented repair-command interface used in remediation lines everywhere
